model Profile {
  id          Int       @id @default(autoincrement())
  name        String
  description String
  projects    Project[]
  links       ProfileLink[]
  experience  Experience[]
  skills      Skill[]
}