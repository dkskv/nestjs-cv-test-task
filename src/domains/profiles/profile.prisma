model Profile {
  id          Int       @id @default(autoincrement())
  name        String
  description String    @default("")
  projects    Project[]
  links       ProfileLink[]
  experiences  Experience[]
  skills      Skill[]
}